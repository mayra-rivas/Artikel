app.directive('about', [function() {
	//returns the html template for the carousel
  return {
    templateUrl: 'directives/about.html',
    controller: ["$scope", "$location", "$http", "$window", function($scope, $location, $http, $window){
 	

   }]}
}]);




